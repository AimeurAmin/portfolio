import styled from "styled-components";

export const StyledApp = styled.div`
  height: 100%;
  background-color: #1F2125;
  .app-container {
    display: flex;
    height: fit-content;
    padding: calc(25 / 16 * 0.8rem);
  }
`